import { useState } from 'react';
import { ArrowDownRight, ArrowRight, Satellite, ShieldAlert, Sprout, Waves, Wifi } from 'lucide-react';
import { SectionLabel } from '../ui/SectionLabel';

export function ContinuousUnderwriting() {
  const [simulated, setSimulated] = useState(false);
  return <section className={`card continuous-card ${simulated ? 'simulated' : ''}`}><div className="continuous-heading"><div><SectionLabel>HOJA DE RUTA / MONITOREO</SectionLabel><h2>Evaluación continua</h2><p>Convertí una señal cambiante del campo en una acción de financiamiento.</p></div><div className="future-pill"><Wifi size={13} /> {simulated ? 'SIMULACIÓN ACTIVA' : 'MONITOREO FUTURO SIMULADO'}</div></div><div className="timeline"><div className="timeline-step now"><span className="timeline-dot" /><div><small>HOY</small><strong>{simulated ? '67' : '82'} <em>Puntaje</em></strong><span>Exposición segura <b>{simulated ? 'ARS 52M' : 'ARS 76M'}</b></span></div></div><ArrowRight className="timeline-arrow" size={18} /><div className={`timeline-step ${simulated ? 'active' : ''}`}><span className="timeline-dot" /><div><small>+60 DÍAS</small><strong>{simulated ? '67' : '—'} <em>Puntaje</em></strong><span>{simulated ? 'Revisar próximo desembolso' : 'Monitoreo aún no activo'}</span></div></div></div><div className="signal-grid"><div><Satellite size={16} /><span>Vegetación satelital</span><strong>{simulated ? '−14%' : '—'}</strong></div><div><Waves size={16} /><span>Anomalía de lluvia</span><strong>{simulated ? '−31%' : '—'}</strong></div><div><Sprout size={16} /><span>Humedad del suelo</span><strong>{simulated ? '−22%' : '—'}</strong></div></div>{simulated && <div className="monitor-alert"><ShieldAlert size={17} /><span>Revisar próximo desembolso</span><ArrowDownRight size={16} /></div>}<button type="button" className="simulate-btn" onClick={() => setSimulated((value) => !value)}>{simulated ? 'Reiniciar simulación' : 'Simular +60 días'} <ArrowRight size={15} /></button></section>;
}
