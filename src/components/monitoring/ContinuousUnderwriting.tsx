import { useState } from 'react';
import { ArrowDownRight, ArrowRight, Satellite, ShieldAlert, Sprout, Waves, Wifi } from 'lucide-react';
import { SectionLabel } from '../ui/SectionLabel';

export function ContinuousUnderwriting() {
  const [simulated, setSimulated] = useState(false);
  return <section className={`card continuous-card ${simulated ? 'simulated' : ''}`}><div className="continuous-heading"><div><SectionLabel>ROADMAP / MONITORING</SectionLabel><h2>Continuous underwriting</h2><p>Turn a changing field signal into a financing action.</p></div><div className="future-pill"><Wifi size={13} /> {simulated ? 'SIMULATION ACTIVE' : 'SIMULATED FUTURE MONITORING'}</div></div><div className="timeline"><div className="timeline-step now"><span className="timeline-dot" /><div><small>TODAY</small><strong>{simulated ? '67' : '82'} <em>Score</em></strong><span>Safe exposure <b>{simulated ? 'ARS 52M' : 'ARS 76M'}</b></span></div></div><ArrowRight className="timeline-arrow" size={18} /><div className={`timeline-step ${simulated ? 'active' : ''}`}><span className="timeline-dot" /><div><small>+60 DAYS</small><strong>{simulated ? '67' : '—'} <em>Score</em></strong><span>{simulated ? 'Review next disbursement' : 'Monitoring not yet active'}</span></div></div></div><div className="signal-grid"><div><Satellite size={16} /><span>Satellite vegetation</span><strong>{simulated ? '−14%' : '—'}</strong></div><div><Waves size={16} /><span>Rainfall anomaly</span><strong>{simulated ? '−31%' : '—'}</strong></div><div><Sprout size={16} /><span>Soil moisture</span><strong>{simulated ? '−22%' : '—'}</strong></div></div>{simulated && <div className="monitor-alert"><ShieldAlert size={17} /><span>Review next disbursement</span><ArrowDownRight size={16} /></div>}<button type="button" className="simulate-btn" onClick={() => setSimulated((value) => !value)}>{simulated ? 'Reset simulation' : 'Simulate +60 days'} <ArrowRight size={15} /></button></section>;
}
